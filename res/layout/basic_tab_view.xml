<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/main_tab_normal" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_centerInParent="true"
        android:background="@drawable/main_tab_picture"
        android:gravity="center"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/tab_imagebtn_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/ic_tab_talk_picture"
            android:textColor="@color/tab_indicator_text" />

        <TextView
            android:id="@+id/tab_btn_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/tab_imagebtn_view"
            android:singleLine="true"
            android:textColor="@color/tab_indicator_text"
            android:textSize="11dip" />
    </LinearLayout>

</RelativeLayout>